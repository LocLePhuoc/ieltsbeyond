services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: ieltsbeyond
      POSTGRES_USER: ieltsbeyond
      POSTGRES_PASSWORD: ieltsbeyond
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
